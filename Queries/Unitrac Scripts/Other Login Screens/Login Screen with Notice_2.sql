USE [UniTrac]
GO 

--Loan Screen Information - LOAN/COLLATERAL/PROPERTY/OWNER/REQUIRED COVERAGE
SELECT * FROM LOAN L
INNER JOIN dbo.FORCE_PLACED_CERTIFICATE FPC ON FPC.LOAN_ID = L.ID
INNER JOIN dbo.FORCE_PLACED_CERT_REQUIRED_COVERAGE_RELATE FPCR ON FPCR.FPC_ID = FPC.ID
INNER JOIN dbo.REQUIRED_COVERAGE RC ON RC.ID = FPCR.REQUIRED_COVERAGE_ID
INNER JOIN dbo.NOTICE_REQUIRED_COVERAGE_RELATE NR ON NR.REQUIRED_COVERAGE_ID = RC.ID
INNER JOIN dbo.NOTICE N ON N.ID = NR.NOTICE_ID
INNER JOIN dbo.CPI_QUOTE CQ ON CQ.ID = N.CPI_QUOTE_ID 
INNER JOIN dbo.CPI_ACTIVITY CA ON CA.CPI_QUOTE_ID = CQ.ID
WHERE LL.CODE_TX IN ('') AND L.NUMBER_TX IN ('')



SELECT  *
FROM    dbo.COLLATERAL_CODE
WHERE   DESCRIPTION_TX LIKE '%%'
        AND AGENCY_ID = '1'
        AND ID IN (  )

SELECT * FROM dbo.LENDER
WHERE CODE_TX IN ('')

SELECT LO.TYPE_CD, LO.CODE_TX , Lo.NAME_TX,*
FROM LENDER L
INNER JOIN LENDER_ORGANIZATION LO ON L.ID = LO.LENDER_ID
INNER JOIN RELATED_DATA RD ON LO.ID = RD.RELATE_ID --AND RD.DEF_ID = '105'
WHERE L.CODE_TX = '' AND Lo.TYPE_CD = 'DIV'




