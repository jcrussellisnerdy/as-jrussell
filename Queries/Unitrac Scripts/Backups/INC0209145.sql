--Loan Screen Information - LOAN/COLLATERAL/PROPERTY/OWNER/REQUIRED COVERAGE
SELECT DISTINCT L.NUMBER_TX, L.ID LOAN_ID, Rc.SUMMARY_STATUS_CD CPI_STATUS, L.STATUS_CD LOAN_STATUS, L.CREATE_DT FROM LOAN L
INNER JOIN COLLATERAL C ON L.ID = C.LOAN_ID
INNER JOIN PROPERTY P ON C.PROPERTY_ID = P.ID
INNER JOIN REQUIRED_COVERAGE RC ON P.ID = RC.PROPERTY_ID
INNER JOIN OWNER_LOAN_RELATE OL ON L.ID = OL.LOAN_ID
INNER JOIN OWNER O ON OL.OWNER_ID = O.ID
INNER JOIN OWNER_ADDRESS OA ON O.ADDRESS_ID = OA.ID
WHERE L.LENDER_ID IN (442) AND RC.SUMMARY_STATUS_CD = 'F' AND L.CREATE_DT > '2012-01-01'
AND L.STATUS_CD = 'A'
ORDER BY L.CREATE_DT ASC

SELECT * FROM dbo.LENDER
WHERE CODE_TX IN ('1952') 



SELECT  *
FROM    dbo.COLLATERAL_CODE
WHERE   DESCRIPTION_TX LIKE '%%'
        AND AGENCY_ID = '1'
        AND ID IN (  )

SELECT * FROM dbo.REF_CODE
WHERE CODE_CD IN ('B','C','E','F','X')
AND DOMAIN_CD LIKE 'required%'

DESCRIPTION_TX LIKE '%CPI Inforce%'



SELECT LO.TYPE_CD, LO.CODE_TX , Lo.NAME_TX,*
FROM LENDER L
INNER JOIN LENDER_ORGANIZATION LO ON L.ID = LO.LENDER_ID
INNER JOIN RELATED_DATA RD ON LO.ID = RD.RELATE_ID --AND RD.DEF_ID = '105'
WHERE L.CODE_TX = '2120' AND Lo.TYPE_CD = 'DIV'