USE [UniTrac]
GO 


--Loan Screen Information - LOAN/COLLATERAL/PROPERTY/OWNER/REQUIRED COVERAGE
SELECT DISTINCT BIA_ID, L.NUMBER_TX INTO #tmp1 FROM LOAN L
INNER JOIN COLLATERAL C ON L.ID = C.LOAN_ID
INNER JOIN PROPERTY P ON C.PROPERTY_ID = P.ID
INNER JOIN REQUIRED_COVERAGE RC ON P.ID = RC.PROPERTY_ID
INNER JOIN OWNER_LOAN_RELATE OL ON L.ID = OL.LOAN_ID
INNER JOIN OWNER O ON OL.OWNER_ID = O.ID
INNER JOIN OWNER_ADDRESS OA ON O.ADDRESS_ID = OA.ID
INNER JOIN dbo.LENDER LL ON LL.ID = L.LENDER_ID
WHERE LL.CODE_TX IN ('8500') 

--DROP TABLE #tmp1

SELECT     T.NUMBER_TX [Loan Number],    NAME_TX ,
        AGENT_TX ,
        PHONE_TX ,
        PHONE_EXT_TX ,
        FAX_TX ,
        FAX_EXT_TX ,
        EMAIL_TX ,
        LINE_1_TX ,
        LINE_2_TX ,
        CITY_TX ,
        STATE_PROV_TX ,
        COUNTRY_TX ,
        POSTAL_CODE_TX
		INTO jcs..INC0302515
		 FROM dbo.BORROWER_INSURANCE_AGENCY BIA
JOIN dbo.ADDRESS A ON A.ID = BIA.ADDRESS_ID
JOIN #tmp1 T ON T.BIA_ID = BIA.ID


SELECT * FROM dbo.BORROWER_INSURANCE_COMPANY_ADDRESS_RELATE

SELECT * FROM dbo.BORROWER_INSURANCE_c
