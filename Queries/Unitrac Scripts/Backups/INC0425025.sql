USE UniTrac


--DROP TABLE jcs..INC0338394

SELECT   DISTINCT
  L.NUMBER_TX ,RC.TYPE_CD [Coverage Type] ,
LO.NAME_TX,
        RC3.DESCRIPTION_TX [Loan Status] ,
        RC8.DESCRIPTION_TX [Collateral Type] , O.LAST_NAME_TX, O.FIRST_NAME_TX, OA.LINE_1_TX, OA.LINE_2_TX, OA.CITY_TX, OA.STATE_PROV_TX, OA.POSTAL_CODE_TX
--INTO    jcs..INC0338394
FROM    LOAN L
        INNER JOIN COLLATERAL C ON L.ID = C.LOAN_ID
        INNER JOIN PROPERTY P ON C.PROPERTY_ID = P.ID
        INNER JOIN REQUIRED_COVERAGE RC ON P.ID = RC.PROPERTY_ID
        INNER JOIN OWNER_LOAN_RELATE OL ON L.ID = OL.LOAN_ID
        INNER JOIN OWNER O ON OL.OWNER_ID = O.ID
        INNER JOIN OWNER_ADDRESS OA ON O.ADDRESS_ID = OA.ID
        INNER JOIN dbo.LENDER LL ON LL.ID = L.LENDER_ID
        INNER JOIN dbo.LENDER_PRODUCT LP ON LP.ID = RC.LENDER_PRODUCT_ID
        INNER JOIN dbo.REF_CODE RC2 ON RC2.CODE_CD = L.RECORD_TYPE_CD
                                       AND RC2.DOMAIN_CD = 'RecordType'
        INNER JOIN dbo.REF_CODE RC3 ON RC3.CODE_CD = L.STATUS_CD
                                       AND RC3.DOMAIN_CD = 'LoanStatus'        
		INNER JOIN dbo.REF_CODE RC8 ON RC8.CODE_CD = C.STATUS_CD
                                       AND RC8.DOMAIN_CD = 'CollateralStatus'
		LEFT JOIN dbo.LENDER_ORGANIZATION LO ON LO.LENDER_ID = LL.ID 
									    AND LO.TYPE_CD = 'DIV' AND LO.CODE_TX = L.DIVISION_CODE_TX	
										WHERE LL.CODE_TX = '4765' AND 
										(L.STATUS_CD = 'U' OR C.STATUS_CD = 'U')