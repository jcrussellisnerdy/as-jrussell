USE UniTrac


SELECT * FROM dbo.LENDER_PAYEE_CODE_FILE LPC
INNER JOIN dbo.LENDER L ON L.ID = LPC.LENDER_ID
INNER JOIN dbo.ADDRESS A ON A.ID = LPC.PAYEE_ADDRESS_ID
WHERE L.CODE_TX = '2725'
AND LPC.PAYEE_CODE_TX IN ('AMICA', 'ELEC')



UPDATE dbo.ADDRESS
SET POSTAL_CODE_TX = '02865', LOCK_ID= LOCK_ID+1,
UPDATE_DT = GETDATE(), UPDATE_USER_TX = 'INC0226881'
WHERE ID IN (268971)